# set scope (@this-oliver sets the package to private)
@this-oliver:registry=https://npm.pkg.github.com

# add github token (add GITHUB_TOKEN to your environment variables)
//npm.pkg.github.com/:_authToken=${GITHUB_TOKEN}

# template config
shamefully-hoist=true
auto-install-peers=true
